---
title: Changelog
description: Track the latest product updates, improvements, and fixes across Documentation.AI.
---

<Update label="2026-09-16" description="MCP Authoring" tags={["feature"]}>
## What changed

**MCP Authoring**
- Connect AI assistants like Claude and ChatGPT to read, edit, review, and publish documentation through the Model Context Protocol
- Edits are applied to a live collaborative workspace and attributed to your signed-in user and AI client — other editors see changes in real time
- All changes remain unpublished until you explicitly call the publish tool
- OAuth authentication provides full authoring access for users with editor or admin role; API keys remain read-only
- Role permissions are rechecked on every tool call, so access changes take effect immediately

**New MCP tools**
- `list_projects` and `select_project` for discovering and targeting documentation projects
- `list_pages`, `get_page`, `get_site_config`, `search`, and `fetch` for reading workspace content and deployed site search
- `create_page`, `update_page`, `rewrite_page`, `delete_page`, and `move_page` for editing documentation files
- `update_site_config` for modifying navigation and site configuration using JSON pointers
- `list_changes`, `diff_page`, `publish`, `resolve_conflict`, and `revert_changes` for reviewing and publishing changes
- `list_branches`, `list_pull_requests`, `create_branch`, `delete_branch`, and `merge_branches` for managing working versions

**MCP credentials**
- Organization admins can create, update, and revoke credentials that control MCP client access to documentation projects
- Credentials support named access roles and configurable expiration (30d, 60d, 90d, 180d, 1y, or custom)

**Renamed and removed**
- `search_documentation` tool renamed to `search` with improved structured output
- `update_documentation` tool removed and replaced by the individual authoring, review, and publish tools above
</Update>

<Update label="2026-09-07" description="Latest Updates" tags={["feature", "improvement", "fix"]}>
## What changed

**Media Manager**
- Upload and manage images, video, and other files in a dedicated media library
- Access media files from a sidebar panel directly in the editor
- Drag and drop multiple files at once, including within read-only contexts
- View inline previews for PDFs and videos
- Replace existing media files while preserving page references
- Monitor storage usage with plan-based quotas and upgrade options
- Track media changes with audit logging
- Compact thumbnails in list views and search results

**Reusable Snippets**
- Create reusable `.mdx` and `.jsx` snippet files from the editor file tree
- Insert snippets into any page using the `/snippet` slash command
- Preview snippets with live JSX compilation before insertion
- Snippets are automatically included in site builds and deployments
- Snippet content is indexed and searchable alongside your pages
- Caching and dependency detection keep snippet previews fresh
- Add optional descriptions when creating snippets

**Search Analytics**
- Track search queries and reader behavior with a new analytics dashboard
- View search volume trends, event tables, and result performance reports
- Privacy-first tracking with debounced and validated events
- Access analytics via API for custom integrations

**Collaboration & Auth**
- Rename and move files across directories with full destination path support
- Role-based access control now enforced for signed-in readers with cached delivery
- Private documentation pages served from cache for authenticated readers
- OAuth redirect URI now resolves from forwarded host for reverse-proxy compatibility
- Performance improvements through per-request config and session deduplication

**Platform Fixes**
- Fixed video player preload behavior when no poster image is set
- Webhook deploys now only trigger for branch refs, not tags
- MDX cache tagging improved for deterministic revalidation
- Sidebar animation transition duration corrected
</Update>